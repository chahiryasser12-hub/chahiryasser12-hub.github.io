# SVG hover color fix

- Calculator SVG artwork now remains dark navy (`#0b1020`) on mouse hover and keyboard focus.
- The icon container uses a soft green background instead of turning navy.
- Gold and green SVG accent colors remain unchanged.
- External SVG image assets keep their original colors with no filter or opacity effect.
- Service-worker cache increased to v19 so browsers load the corrected CSS.
