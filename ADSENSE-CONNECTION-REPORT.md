# AdSense connection package

- Added the AdSense connection script to the `<head>` of all 40 HTML pages.
- Publisher client: `ca-pub-8379415024436818`.
- Added root `ads.txt` with the supplied authorized seller line.
- Confirmed there is exactly one AdSense loader on every HTML page.
- Kept all local CSS links and mobile layout fixes.
- Updated the service-worker cache to `fynzo-v11`.

This package connects the site for AdSense review. It does not add fake ad boxes or manual ad units.
