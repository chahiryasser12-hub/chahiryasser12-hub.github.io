# Final asset fixes

- Added all 23 Open Graph PNG files required by the HTML pages.
- Confirmed every Open Graph and Twitter image reference resolves locally.
- Confirmed every Open Graph PNG is 1200 x 630 pixels.
- Deleted `ads.txt` because the site has not yet received a real AdSense publisher ID.
- Updated `manifest.json` to use the existing `favicon.svg` icon.
- Deleted the redundant `site.webmanifest` file.
